# 2026-09-23 整站內容與操作檢查

## 結論與發布狀態

本機工作版本為 **517 件（新增 500＋既有校正／分案 17）**。正式站與剛更新的 GitHub origin/main 仍為 **171 件**；讀取型 API 比對 191／191 一致，但正式站 version.json 是 e0f0c694，GitHub 為 60f7073a，不能稱整個部署版本一致。本次未 commit、push 或 deploy。

「可以修改上線」隨後被使用者更正為「上限」，當時已停止發布流程；後續授權涵蓋所有模型逾時、修改分析頁及整站檢查。目前已完成可供檢視的本機修正，發布須另確認。此界線不是套用額外 skill 核准規則。

## 資料與分析修正

- 移除假的載入失敗預設數字；分析由實際 API 取得。
- 顯示 515 件核心查核、2 件裁決主文核對，與獨立模型來源複核分開。
- 多模型來源對讀歷史覆蓋為不重複 15 件：Codex 11、Gemini 8、Grok 8；8 件至少兩模型、4 件三模型，Claude 尚無有效回覆。不是 500 件全部獨立複審。
- 對 252 件只有公告標題支持的國家／地區加待補證標示。公開資料沒有標示台灣／中華台北選手，但無法宣稱這項排除已逐件獨立核實。
- 統計圖完整保留分母或標明只畫前十項，未知成績／獎牌結果不計成「無」。年份是收錄事件／裁決年份，不是年度發生率。
- 公開來源以第一來源網址的發布機構分組，避免混入名冊中的主管總會：USADA 267、AIU 123、ITA 108、其他 14、ITIA 3、CAS 2。
- Paparella 的「服刑」改為禁賽執行，釐清追溯 TUE 未准及清單變更終止剩餘禁賽。
- 時間軸與研究數字附官方／原始研究來源，區分 TUE、奪牌觀察性關聯與特定補充品樣本，移除無來源的政策因果與市場污染率推估。

歷史模型回覆與舊資料雜湊保留不改；新資料雜湊與修後變更見 `post-audit-dataset.json`。沒有把舊審核偽裝成修正後再跑一遍。

## 全站內容與 UX 修正

1. 新聞移除台灣選手與未有充分來源支持的案例，改為有來源的 WADA 更新。
2. 校正 2026 S3 劑量、P1 運動清單、testosterone TUE、S9 殘留與清除期、S4/S6 混合分類、M2 醫療例外及偽麻黃鹼濃度門檻。同步 API、TUE 工具與測驗，詳見 `content-sources.md`。
3. 專科教材改讀 API 共用資料；移除 Codeine 被列為 S7、克洛米芬當作免禁用替代、輸血有醫療監督便允許等誤導。必要胰島素不可自行用飲食或口服藥替代。
4. ADeL／測驗說明更新 115 年報名條件，藥檢流程補上延遲許可、未成年代表、檢體編號與匿名實驗室表單；移除無分母的 90%／10% 圖卡。
5. 案例搜尋修正清除未重載、舊請求覆蓋、失敗狀態殘留；詳情頁區分找不到與伺服器失敗並可重試。
6. TUE 查詢阻止 Enter 重複送出與舊結果覆蓋；教育違規類型有錯誤重試；選單 Escape 會關閉並還原焦點；表單補上可讀標籤。
7. 回饋前後端接受一致格式，保存頁面與選填聯絡資料；未存入 D1 回 503、超額 429，畫面不顯示假成功。加上同站 JSON、32KiB 串流上限與原子性流量限制；沒有新增資料庫 schema。
8. 套件安全修補並更新 React Router 至 7.18.4、Vite 7.3.6、Axios 1.20.0；根目錄與前端 npm audit 均 0。Node 要求改為 22.13 以上，README 改成目前 Pages Functions 架構。

獨立複驗發現「公開來源／主管機構混淆」及手機測驗結果保持舊捲動位置，均已列入本次修正與回歸。

## 驗證紀錄

- 案例／Pages API／教育規則：34 項通過；含全部 517 件詳情路由、分頁、重建一致性、資料分母、回饋並行限制與實際 TUE 資料判定。
- 前端純邏輯：39 項通過。
- 舊 Express 後端：60 項通過；首次沙箱因本機連接埠 EPERM 失敗，獲准於沙箱外重跑通過。
- 前端 ESLint、正式建置（11 個 SEO HTML）、資料完成檢查及 git diff --check 通過。
- 最終本機 bundle `2026-09-23T08:59:04.082Z`：26／26 路由尺寸檢查、12／12主要互動、19／19 錯誤與競態恢復、8／8 回饋 UI 全數通過；手機測驗分數與焦點可見性也通過。
- 瀏覽器最新結果另見 `site-browser.json`、`ux-regression.json`、`feedback-browser.json`；每份記錄實際 bundle 時間／雜湊。桌機 1280 與手機 390，測驗、圖表數值、搜尋、TUE、導覽、錯誤恢復及模擬回饋。
- 本地模擬回饋全數攔截；沒有寫入正式站回饋資料。
- 正式站與 GitHub 191 項比對見 `remote-verification-2026-09-23.json`，版本戳記不符明確列為未通過，不因 API 一致掩蓋。

## 查核限制

這是全站頁面與主要操作流程檢查，加上列明規則的官方來源校正，不代表每個字、外部連結內容、所有藥品商品或全部 500 件完整仲裁卷宗都經獨立複審。也未測真實 D1 寫入、Safari／Firefox、真實輔助科技或所有小於 390px 螢幕。既有 141 件封存待查資料與新增 500 是不同範圍，未重新放入公開 API。

所有後續 multi-LLM 審核模型預設上限改為 600 秒；不改寫以前 180 秒逾時歷史，也沒有自動重跑全部模型。政策見 `docs/research/2026-09-23-multi-llm-audit/runtime-policy.json`。
